# Backend (api + crawl + creative pipeline)
# setuptools: distutils shim on Python 3.12+ (undetected-chromedriver imports distutils.version)
setuptools>=69.0
fastapi
python-multipart
uvicorn[standard]
python-dotenv
openai
requests
httpx
beautifulsoup4
tenacity
selenium
undetected-chromedriver
pillow
celery
gevent
redis
tenacity

# API: SQLAlchemy + auth (install one DB driver: pymysql for MySQL, psycopg[binary] for PostgreSQL)
sqlalchemy>=2.0
alembic>=1.13
passlib[bcrypt]
bcrypt==4.0.1
python-jose[cryptography]
pymysql
# PostgreSQL: pip install "psycopg[binary]" and set DATABASE_URL=postgresql+psycopg://user:pass@host/dbname
